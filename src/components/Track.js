import React from "react"
//import {map, pipe, prop, addIndex} from "ramda";

/* const createVar = pipe(
    prop("variants"),
    addIndex(map)((variant, i) => (
        <span className="variant" key={`variant-${i}`}>{i}</span>
    ))
        /* file: `drum-0${i}.mp3`,
            active: i === 0,
            index : i */
    ); */


export default props => (
    <span className="track">

    </span>
);
